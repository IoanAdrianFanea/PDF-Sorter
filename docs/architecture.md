# Architecture Overview

Flow:
User → Frontend → Backend → Storage → Database → Search Results

Components:
- Frontend: UI for upload + search
- Backend: API + processing logic
- Storage: local PDF files
- Database: metadata + extracted text